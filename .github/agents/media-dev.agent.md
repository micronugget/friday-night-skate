* **Role:** Handling VideoJS, YouTube API, and GPS metadata extraction.
* **Focus:** Writing PHP for custom media source plugins.
* **Instruction:** *"You must use `exif_read_data()` or `ffprobe` (via DDEV) to capture GPS before remote upload."*